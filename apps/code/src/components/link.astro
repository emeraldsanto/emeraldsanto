---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'a'>;

const { class: className, ...rest } = Astro.props;
---

<a {...rest} class:list={['decoration-maroon-oak decoration-1 hover:decoration-crown-of-thorns duration-200 transition-all underline', className]}><slot /></a>
