---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'a'>;

const { class: className, ...rest } = Astro.props;
---

<a {...rest} class:list={['duration-200 hover:text-indigo-900 text-slate-700 transition-all underline', className]}><slot /></a>
